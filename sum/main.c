#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sum=0, a,b;
    printf("Enter 2 values you want to add");

    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("your sum result is %d",sum);
    getch();
}
